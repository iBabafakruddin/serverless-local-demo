{
    "name": "stock-market-india",
    "version": "1.0.0",
    "description": "\"An API Library which fetches data from Bombay Stock Exchange and National Stock Exchange and returns data in JSON format.\"",
    "main": "app.js",
    "scripts": {
        "start": "serverless offline start --provider aws --key DUMMY-LOCAL-KEY --secret DUMMY-LOCAL-SECRET",
        "test": "echo \"Error: no test specified\" && exit 1",
        "install-local-db":"serverless dynamodb install"
    },
    "devDependencies": {
        "aws-sdk": "^2.758.0",
        "express": "^4.17.1",
        "serverless-dynamodb-local": "^0.2.39",
        "serverless-http": "^2.6.0",
        "serverless-offline": "^6.8.0"
    }
}
